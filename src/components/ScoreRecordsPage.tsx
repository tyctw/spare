import { useEffect, useState } from 'react';
import { ArrowLeft, BookOpen, CalendarDays, Check, ClipboardPlus, Crown, LogIn, Save, Trash2 } from 'lucide-react';
import { callBackend } from '../lib/api';
import { consumeLineLoginCodeFromFragment } from '../lib/membership';
import { withBasePath } from '../lib/routes';

type Scores = { chinese: string; english: string; math: string; science: string; social: string; composition: number };
type RecordItem = { id: string; record_type: 'mock' | 'official'; title: string; exam_date: string | null; region: string | null; scores: Scores; created_at: string };
const emptyScores: Scores = { chinese: '', english: '', math: '', science: '', social: '', composition: 0 };
const gradeOptions = ['A++', 'A+', 'A', 'B++', 'B+', 'B', 'C'];
const subjects: Array<[keyof Omit<Scores, 'composition'>, string]> = [['chinese', '國文'], ['english', '英文'], ['math', '數學'], ['science', '自然'], ['social', '社會']];
const scoreValue = (grade: string) => ({ 'A++': 7, 'A+': 6, A: 5, 'B++': 4, 'B+': 3, B: 2, C: 1 }[grade] || 0);
const recordScore = (record: RecordItem) => subjects.reduce((total, [key]) => total + scoreValue(record.scores[key]), 0) + Number(record.scores.composition || 0);

export default function ScoreRecordsPage() {
  const [loggedIn, setLoggedIn] = useState(false); const [name, setName] = useState(''); const [records, setRecords] = useState<RecordItem[]>([]);
  const [recordType, setRecordType] = useState<'mock' | 'official'>('mock'); const [title, setTitle] = useState(''); const [examDate, setExamDate] = useState(''); const [region, setRegion] = useState(''); const [scores, setScores] = useState<Scores>(emptyScores); const [saving, setSaving] = useState(false); const [notice, setNotice] = useState('');
  const load = async () => { const result = await callBackend<{ loggedIn: boolean; name?: string; records: RecordItem[] }>({ action: 'getMemberScoreRecords' }); setLoggedIn(result.loggedIn); setName(result.name || ''); setRecords(result.records || []); };
  useEffect(() => { void (async () => { try { await consumeLineLoginCodeFromFragment(); await load(); } catch { setNotice('無法讀取帳號資料，請稍後再試。'); } })(); }, []);
  const login = () => { const url = (import.meta.env.VITE_SUPABASE_URL || '').replace(/\/$/, ''); if (!url) { setNotice('尚未設定 LINE 登入服務。'); return; } window.location.assign(`${url}/functions/v1/line-login?returnTo=/score-records`); };
  const save = async () => { if (!title.trim()) { setNotice('請替這筆成績取一個名稱，例如：第一次模擬考。'); return; } if (subjects.some(([key]) => !scores[key])) { setNotice('請完整填寫五科等級。'); return; } setSaving(true); setNotice(''); try { await callBackend({ action: 'saveMemberScoreRecord', recordType, title, examDate: examDate || null, region, scores }); setTitle(''); setExamDate(''); setRegion(''); setScores(emptyScores); await load(); setNotice('已安全儲存到你的帳號。'); } catch (error) { setNotice(error instanceof Error ? error.message : '儲存失敗，請稍後再試。'); } finally { setSaving(false); } };
  const remove = async (id: string) => { if (!window.confirm('確定要刪除這筆成績紀錄嗎？')) return; try { await callBackend({ action: 'deleteMemberScoreRecord', id }); await load(); setNotice('已刪除成績紀錄。'); } catch (error) { setNotice(error instanceof Error ? error.message : '刪除失敗，請稍後再試。'); } };
  return <main className="min-h-screen bg-[#f5f6ff] px-4 py-6 text-slate-900 sm:px-6 sm:py-10"><div className="mx-auto max-w-5xl"><a href={withBasePath('/')} className="inline-flex items-center gap-2 rounded-xl border-2 border-slate-900 bg-white px-4 py-2 text-sm font-black shadow-[2px_2px_0_#161b35]"><ArrowLeft className="h-4 w-4" />回到落點分析</a><header className="mt-5 rounded-[2rem] border-2 border-slate-900 bg-sky-100 p-5 shadow-[5px_5px_0_#161b35] sm:p-7"><div className="flex items-start gap-4"><span className="grid h-12 w-12 shrink-0 place-items-center rounded-2xl border-2 border-slate-900 bg-white text-sky-700"><ClipboardPlus className="h-6 w-6" /></span><div><p className="text-xs font-black tracking-[.15em] text-sky-700">MY SCORE RECORDS</p><h1 className="mt-1 text-3xl font-black tracking-tight sm:text-4xl">我的成績紀錄</h1><p className="mt-2 max-w-2xl text-sm font-bold leading-6 text-slate-600">登入後可保存模擬考與正式會考成績，方便比較自己的準備趨勢。請勿輸入准考證號、姓名或其他不必要個資。</p></div></div></header>{!loggedIn ? <section className="mt-5 rounded-[2rem] border-2 border-slate-900 bg-white p-6 text-center shadow-[4px_4px_0_#161b35]"><Crown className="mx-auto h-8 w-8 text-amber-600" /><h2 className="mt-3 text-xl font-black">登入帳號，保存你的成績</h2><p className="mt-2 text-sm font-bold leading-6 text-slate-600">使用 LINE 登入即可建立個人紀錄，不需要另設密碼，也不必是付費會員。</p><button type="button" onClick={login} className="mt-5 inline-flex items-center gap-2 rounded-xl border-2 border-slate-900 bg-sky-400 px-5 py-3 font-black shadow-[3px_3px_0_#161b35]"><LogIn className="h-4 w-4" />使用 LINE 登入</button></section> : <><section className="mt-5 rounded-[2rem] border-2 border-slate-900 bg-white p-5 shadow-[4px_4px_0_#161b35] sm:p-6"><div className="flex items-center justify-between gap-3"><div><p className="text-xs font-black tracking-[.14em] text-sky-700">ACCOUNT READY</p><h2 className="mt-1 text-xl font-black">{name || '你的'}成績紀錄</h2></div><span className="inline-flex items-center gap-1 rounded-full bg-emerald-100 px-3 py-1.5 text-xs font-black text-emerald-800"><Check className="h-3.5 w-3.5" />已登入</span></div><div className="mt-5 grid gap-4 md:grid-cols-2"><label className="text-sm font-black">成績類型<select value={recordType} onChange={(event) => { const type = event.target.value as 'mock' | 'official'; setRecordType(type); if (!title) setTitle(type === 'mock' ? '模擬考' : '正式會考'); }} className="mt-2 w-full rounded-xl border-2 border-slate-300 bg-slate-50 px-3 py-2.5 font-bold"><option value="mock">模擬考成績</option><option value="official">正式會考成績</option></select></label><label className="text-sm font-black">這筆成績的名稱<input value={title} onChange={(event) => setTitle(event.target.value.slice(0, 60))} placeholder="例如：第一次模擬考" className="mt-2 w-full rounded-xl border-2 border-slate-300 bg-slate-50 px-3 py-2.5 font-bold" /></label><label className="text-sm font-black">考試日期（選填）<input type="date" value={examDate} onChange={(event) => setExamDate(event.target.value)} className="mt-2 w-full rounded-xl border-2 border-slate-300 bg-slate-50 px-3 py-2.5 font-bold" /></label><label className="text-sm font-black">就學區（選填）<input value={region} onChange={(event) => setRegion(event.target.value.slice(0, 40))} placeholder="例如：桃連區" className="mt-2 w-full rounded-xl border-2 border-slate-300 bg-slate-50 px-3 py-2.5 font-bold" /></label></div><div className="mt-5 grid gap-3 sm:grid-cols-2 lg:grid-cols-3">{subjects.map(([key, label]) => <label key={key} className="rounded-xl border-2 border-slate-200 bg-slate-50 p-3 text-sm font-black">{label}<select value={scores[key]} onChange={(event) => setScores((current) => ({ ...current, [key]: event.target.value }))} className="mt-2 w-full rounded-lg border-2 border-slate-300 bg-white px-2 py-2 font-bold"><option value="">選擇等級</option>{gradeOptions.map((grade) => <option key={grade}>{grade}</option>)}</select></label>)}<label className="rounded-xl border-2 border-slate-900 bg-slate-900 p-3 text-sm font-black text-white">寫作<select value={scores.composition} onChange={(event) => setScores((current) => ({ ...current, composition: Number(event.target.value) }))} className="mt-2 w-full rounded-lg border-2 border-slate-700 bg-white px-2 py-2 font-bold text-slate-900">{[0, 1, 2, 3, 4, 5, 6].map((score) => <option key={score} value={score}>{score} 級分</option>)}</select></label></div><button type="button" onClick={save} disabled={saving} className="mt-5 inline-flex w-full items-center justify-center gap-2 rounded-xl border-2 border-slate-900 bg-sky-400 px-4 py-3.5 font-black shadow-[3px_3px_0_#161b35] disabled:opacity-50"><Save className="h-4 w-4" />{saving ? '儲存中…' : '儲存這筆成績'}</button></section><ScoreHistory records={records} onRemove={remove} />{records.length > 0 && <ScoreInsights records={records} />}</>}{notice && <p role="status" className="mt-4 rounded-xl border-2 border-slate-900 bg-amber-50 p-3 text-sm font-bold text-slate-800">{notice}</p>}</div></main>;
}

function ScoreHistory({ records, onRemove }: { records: RecordItem[]; onRemove: (id: string) => void }) { return <section className="mt-5 rounded-[2rem] border-2 border-slate-900 bg-white p-5 shadow-[4px_4px_0_#161b35] sm:p-6"><h2 className="flex items-center gap-2 text-xl font-black"><BookOpen className="h-5 w-5 text-sky-700" />已儲存的成績</h2>{records.length ? <div className="mt-4 grid gap-3 md:grid-cols-2">{records.map((record) => <article key={record.id} className="rounded-2xl border-2 border-slate-200 bg-slate-50 p-4"><div className="flex items-start justify-between gap-3"><div><span className={`rounded-full px-2 py-1 text-xs font-black ${record.record_type === 'official' ? 'bg-emerald-100 text-emerald-800' : 'bg-amber-100 text-amber-800'}`}>{record.record_type === 'official' ? '正式會考' : '模擬考'}</span><h3 className="mt-2 font-black">{record.title}</h3><p className="mt-1 flex items-center gap-1 text-xs font-bold text-slate-500"><CalendarDays className="h-3.5 w-3.5" />{record.exam_date || new Date(record.created_at).toLocaleDateString('zh-TW')}{record.region ? ` · ${record.region}` : ''}</p></div><button type="button" onClick={() => onRemove(record.id)} className="rounded-lg border border-rose-200 bg-white p-2 text-rose-700"><Trash2 className="h-4 w-4" /></button></div><p className="mt-3 rounded-xl bg-white px-3 py-2 text-sm font-black text-slate-700">國 {record.scores.chinese}　英 {record.scores.english}　數 {record.scores.math}　自 {record.scores.science}　社 {record.scores.social}　寫作 {record.scores.composition}</p></article>)}</div> : <p className="mt-4 rounded-xl border-2 border-dashed border-slate-300 bg-slate-50 p-6 text-center text-sm font-bold text-slate-500">尚未儲存成績；新增第一筆模擬考或會考成績吧。</p>}</section>; }

function ScoreInsights({ records }: { records: RecordItem[] }) { const chronological = [...records].reverse(); const latest = chronological.at(-1)!; const previous = chronological.at(-2); const grades = subjects.map(([key]) => latest.scores[key]); const a = grades.filter((grade) => grade.startsWith('A')).length; const b = grades.filter((grade) => grade.startsWith('B')).length; const c = grades.filter((grade) => grade === 'C').length; const max = Math.max(...chronological.map(recordScore), 1); const changes = previous ? subjects.map(([key, label]) => ({ label, value: scoreValue(latest.scores[key]) - scoreValue(previous.scores[key]) })).filter((item) => item.value) : []; return <section className="mt-5 rounded-[2rem] border-2 border-slate-900 bg-white p-5 shadow-[4px_4px_0_#161b35] sm:p-6"><div className="flex flex-col gap-2 sm:flex-row sm:items-end sm:justify-between"><div><p className="text-xs font-black tracking-[.15em] text-indigo-700">SCORE INSIGHTS</p><h2 className="mt-1 text-2xl font-black">成績趨勢與分析</h2><p className="mt-1 text-sm font-bold leading-6 text-slate-600">模擬考難度不同，請把變化當作調整方向，而非錄取預測。</p></div><span className="rounded-xl bg-indigo-100 px-3 py-2 text-sm font-black text-indigo-900">最新：{latest.title}</span></div><div className="mt-5 grid gap-4 lg:grid-cols-[1.2fr_.8fr]"><div className="rounded-2xl border-2 border-slate-900 bg-slate-50 p-4"><h3 className="font-black">整體表現走勢</h3><div className="mt-5 flex h-44 items-end gap-2 border-b-2 border-slate-300 pb-1">{chronological.map((record) => <div key={record.id} className="flex min-w-0 flex-1 flex-col items-center justify-end gap-1"><span className="text-xs font-black text-indigo-800">{recordScore(record)}</span><div className={`w-full rounded-t-lg ${record.record_type === 'official' ? 'bg-emerald-500' : 'bg-indigo-500'}`} style={{ height: `${Math.max(10, (recordScore(record) / max) * 100)}%` }} /><span className="w-full truncate text-center text-[10px] font-bold text-slate-500" title={record.title}>{record.title}</span></div>)}</div><p className="mt-3 text-xs font-bold text-slate-500">總覽分數為五科等級換算值加寫作級分，只供比較個人紀錄。</p></div><div className="rounded-2xl border-2 border-slate-900 bg-amber-50 p-4"><h3 className="font-black">最新成績輪廓</h3><div className="mt-4 grid grid-cols-3 gap-2 text-center"><Stat count={a} label="A 類科目" tone="emerald" /><Stat count={b} label="B 類科目" tone="amber" /><Stat count={c} label="C 類科目" tone="rose" /></div><p className="mt-4 text-sm font-bold leading-6 text-slate-700">{c ? '先從 C 類科目的基礎題與常錯單元開始規劃。' : a >= 3 ? '強項已形成優勢；維持 A 類科目，也找出 B 類科目最常失分的題型。' : '從一個最常失分的科目或單元開始調整，比一次全面加量更容易持續。'}</p></div></div>{previous && <div className="mt-4 rounded-2xl border-2 border-slate-900 bg-sky-50 p-4"><h3 className="font-black">與前一次紀錄相比</h3>{changes.length ? <div className="mt-3 flex flex-wrap gap-2">{changes.map((change) => <span key={change.label} className={`rounded-lg px-3 py-2 text-sm font-black ${change.value > 0 ? 'bg-emerald-100 text-emerald-800' : 'bg-rose-100 text-rose-800'}`}>{change.label} {change.value > 0 ? `+${change.value}` : change.value}</span>)}</div> : <p className="mt-2 text-sm font-bold text-slate-600">五科等級與前一次相同；可改看錯題類型、時間分配與寫作練習。</p>}<p className="mt-3 text-xs font-bold text-slate-500">比較的是等級換算差異；不同模擬考的範圍與難度可能不同。</p></div>}</section>; }

function Stat({ count, label, tone }: { count: number; label: string; tone: 'emerald' | 'amber' | 'rose' }) { const styles = { emerald: 'bg-emerald-100 text-emerald-800', amber: 'bg-amber-100 text-amber-800', rose: 'bg-rose-100 text-rose-800' }; return <div className={`rounded-xl p-3 ${styles[tone]}`}><strong className="block text-2xl font-black">{count}</strong><span className="text-xs font-black">{label}</span></div>; }
